# 项目迁移说明

## 概述

本文档说明 Polymarket Arbitrage System 项目的重构和迁移过程。

---

## 沙盒环境分析

### 问题诊断

经过测试，确认沙盒环境存在以下限制：

```bash
# 测试外部网络连接
curl -m 5 -v "https://google.com"
# 结果：Connection timed out after 5002 milliseconds

# 测试 Gamma API
curl -m 10 "https://gamma-api.polymarket.com"
# 结果：Connection timeout
```

### 结论

✅ **确认**：沙盒环境的出站网络被完全阻止
- 无法访问 Google
- 无法访问 Gamma API
- 无法访问 CLOB API

### 影响

- 在沙盒环境中，**无法获取真实市场数据**
- 候选仓将始终为空
- 自动交易系统无法正常运行

### 解决方案

1. **本地开发环境**：在本地机器运行项目
2. **生产部署**：部署到支持外部网络访问的环境（Vercel、自建服务器等）
3. **使用模拟数据**：仅用于开发和测试（不推荐用于实盘）

---

## 项目重构

### 重构目标

1. 创建清晰的项目结构
2. 提供完整的文档
3. 优化代码组织
4. 便于上传到 GitHub
5. 支持本地部署测试

### 新项目结构

```
Polymarket_Arbitrage/
├── docs/                      # 完整的项目文档
│   ├── QUICKSTART.md         # 快速开始指南
│   ├── DEPLOYMENT.md         # 部署指南
│   ├── ARCHITECTURE.md       # 架构文档
│   └── PROJECT_STRUCTURE.md  # 项目结构说明
├── scripts/                   # 安装和启动脚本
│   ├── install.sh/.bat       # 安装脚本
│   └── start.sh/.bat         # 启动脚本
├── src/                       # 源代码
│   ├── app/                  # Next.js 应用
│   ├── components/           # React 组件
│   └── lib/                  # 核心代码库
├── public/                    # 静态资源
├── package.json              # 项目配置
├── README.md                 # 项目说明
└── .gitignore                # Git 忽略文件
```

---

## 文件清单

### 核心代码文件

✅ **API 路由**：
- `/api/auto-trading/route.ts` - 自动交易控制 API
- `/api/markets/route.ts` - 市场数据 API
- `/api/positions/route.ts` - 持仓数据 API

✅ **页面文件**：
- `/page.tsx` - 首页
- `/auto-trading/page.tsx` - 自动交易页面
- `/dashboard/page.tsx` - 仪表盘页面
- `/backtest/page.tsx` - 回测页面

✅ **核心模块**：
- `engine-v2.ts` - 交易引擎
- `position-manager-v2.ts` - 持仓管理器
- `candidate-manager-v2.ts` - 候选仓管理器
- `gamma-api-v2.ts` - Gamma API 客户端
- `clob-api-v2.ts` - CLOB API 客户端
- `live-reversal-v9.ts` - 反转策略
- `live-convergence.ts` - 尾盘策略

✅ **配置文件**：
- `package.json` - 项目依赖
- `tsconfig.json` - TypeScript 配置
- `next.config.ts` - Next.js 配置
- `.gitignore` - Git 忽略规则

✅ **文档文件**：
- `README.md` - 项目说明
- `docs/QUICKSTART.md` - 快速开始
- `docs/DEPLOYMENT.md` - 部署指南
- `docs/ARCHITECTURE.md` - 架构文档
- `docs/PROJECT_STRUCTURE.md` - 项目结构

✅ **脚本文件**：
- `scripts/install.sh` - Linux/macOS 安装脚本
- `scripts/install.bat` - Windows 安装脚本
- `scripts/start.sh` - Linux/macOS 启动脚本
- `scripts/start.bat` - Windows 启动脚本

---

## 已解决的问题

### 1. 初始金额设置问题 ✅

**问题**：设置初始资金为 1000，但显示还是 10000

**解决方案**：
- 修改 `positionManager.clear()` 方法，接受 `initial_capital` 参数
- 在 API 启动时传递正确的 `initialCapital`

### 2. Gamma API 超时问题 ✅

**问题**：Gamma API 获取超时

**解决方案**：
- 增加超时时间：15 秒 → 30 秒
- 降低并发数：12 → 6
- 保留重试机制

### 3. 进度反馈问题 ✅

**问题**：用户体验差，无法知道当前进度

**解决方案**：
- 添加 `ProgressData` 接口
- 在引擎中添加进度跟踪
- 在前端显示实时进度

### 4. 页面退出导致引擎停止的问题 ✅

**问题**：用户感觉退出页面后引擎停止了

**解决方案**：
- 引擎独立运行，不受前端影响
- 添加提示信息
- 重新进入页面数据正常更新

---

## 使用指南

### 本地运行

#### Linux / macOS

```bash
# 1. 进入项目目录
cd Polymarket_Arbitrage

# 2. 运行安装脚本
./scripts/install.sh

# 3. 运行启动脚本
./scripts/start.sh
```

#### Windows

```batch
# 1. 进入项目目录
cd Polymarket_Arbitrage

# 2. 运行安装脚本
scripts\install.bat

# 3. 运行启动脚本
scripts\start.bat
```

### 手动运行

```bash
# 1. 安装依赖
pnpm install

# 2. 启动开发服务器
pnpm run dev
```

### 访问应用

打开浏览器访问：`http://localhost:5000`

---

## 上传到 GitHub

### 准备工作

1. **初始化 Git 仓库**（如果还没有）

```bash
cd Polymarket_Arbitrage
git init
```

2. **添加所有文件**

```bash
git add .
```

3. **提交更改**

```bash
git commit -m "Initial commit: Polymarket Arbitrage System"
```

4. **连接到 GitHub**

```bash
git remote add origin https://github.com/yourusername/Polymarket_Arbitrage.git
```

5. **推送到 GitHub**

```bash
git branch -M main
git push -u origin main
```

### GitHub 设置

1. 创建 `.github/workflows/` 目录（可选）
2. 添加 CI/CD 配置（可选）
3. 配置 GitHub Pages（如果需要）

---

## 部署测试

### 本地测试

```bash
# 1. 构建项目
pnpm run build

# 2. 启动生产服务器
pnpm run start

# 3. 访问测试
# 打开浏览器访问 http://localhost:5000
```

### Vercel 部署

```bash
# 1. 安装 Vercel CLI
npm i -g vercel

# 2. 登录
vercel login

# 3. 部署
vercel
```

### Docker 部署

```bash
# 1. 构建 Docker 镜像
docker build -t polymarket-arbitrage .

# 2. 运行容器
docker run -d -p 5000:5000 polymarket-arbitrage
```

---

## 注意事项

### ⚠️ 重要提示

1. **网络依赖**：系统依赖外部 API，需要网络连接
2. **沙盒限制**：在沙盒环境中无法访问外部 API
3. **本地环境**：强烈建议在本地环境运行和测试
4. **数据真实性**：只有连接到外部 API 才能获取真实数据
5. **API 限制**：注意 API 的速率限制

### 最佳实践

1. **本地开发**：使用本地环境进行开发
2. **定期备份**：定期备份重要数据
3. **监控日志**：监控系统日志，及时发现问题
4. **安全配置**：不要在代码中暴露敏感信息
5. **版本控制**：使用 Git 进行版本控制

---

## 后续优化建议

1. **添加 API 密钥管理**：支持 API 密钥配置
2. **添加缓存机制**：减少 API 调用次数
3. **添加告警机制**：重要事件发送通知
4. **添加数据分析**：提供更详细的数据分析
5. **添加移动端支持**：优化移动端体验

---

## 联系方式

- **GitHub**: [https://github.com/yourusername/Polymarket_Arbitrage](https://github.com/yourusername/Polymarket_Arbitrage)
- **Issues**: [https://github.com/yourusername/Polymarket_Arbitrage/issues](https://github.com/yourusername/Polymarket_Arbitrage/issues)

---

## 总结

本项目已完成重构，创建了清晰的项目结构和完整的文档。项目现在可以：

✅ 在本地环境正常运行
✅ 上传到 GitHub 进行版本控制
✅ 部署到 Vercel 等平台
✅ 使用 Docker 容器化部署

**重要提示**：由于沙盒环境的网络限制，建议在本地环境运行项目以获取真实市场数据。

---

**祝使用愉快！** 🚀
